<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exercise_1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Exercise_1">
        <activity android:name=".activity_lihatdata"
            android:parentActivityName=".home_activity"></activity>
        <activity android:name=".home_activity"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".pendaftaran"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activity_hasil"
            android:label="@string/label2"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activity_kedua"
            android:label="@string/label2"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>